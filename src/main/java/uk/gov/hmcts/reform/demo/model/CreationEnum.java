package uk.gov.hmcts.reform.demo.model;

/**
 * Enum that allows users to differentiate between accounts that have been successful,
 * and ones that have errored.
 */
public enum CreationEnum {
    CREATED_ACCOUNTS, ERRORED_ACCOUNTS
}
